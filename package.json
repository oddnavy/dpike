{
  "name": "dpike",
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "format": "prettier --write .",
    "upgrade": "npm outdated || true && npx npm-check-updates -u --target latest --timeout 120000 --format group && npm install --no-audit --no-fund --no-optional"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
